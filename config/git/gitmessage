

# Type: short summary of the whole change
#
# - topic -- detail
# - topic -- detail
#
# --NOTES--
# Subject: one line, max 72 chars, followed by a BLANK LINE.
#   Without the blank line git treats the whole message as the subject.
# Body: one bullet per topic. Target 72, max 80.
# Tighten wording before splitting a long bullet.
#   Split only when it is genuinely two topics.
#   Wrap only when it can't split.
# No dates, no file lists -- git stores both.
# Valid types: Docs, Chore, Build, Refactor, Fix
